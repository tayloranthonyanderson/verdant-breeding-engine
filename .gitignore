# R
.Rproj.user
.Rhistory
.RData
.Ruserdata
*.Rcheck/
renv/library/
renv/staging/

# Node / Next.js
node_modules/
.next/
out/
npm-debug.log*
.pnpm-debug.log*

# Env & secrets
.env
.env.*
!.env.example
*.local

# OS
.DS_Store
Thumbs.db

# Local Claude Code tooling (machine-specific paths — keep out of the repo)
.claude/launch.json
.claude/settings.local.json

# Build / data outputs
*.rds
/tmp/
dist/

# Playwright
test-results/
playwright-report/
/Users/**/ms-playwright/

# Large public G2F raw downloads (extract small per-env fixtures instead)
/data/g2f/raw/

# TypeScript incremental build cache
*.tsbuildinfo

# Local scratch: working plans / PRDs / issue drafts (kept on disk, out of the repo)
.scratch/
