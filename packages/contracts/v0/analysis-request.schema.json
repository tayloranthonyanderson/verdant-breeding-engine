{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://verdant.bio/contracts/v0/analysis-request.schema.json",
  "title": "Verdant Analysis Request (v0)",
  "description": "The self-contained input the web tier enqueues for the R compute kernel. The kernel is stateless (ADR-0001): everything a fit needs is here. The request carries the breeder's INTENT, never a model specification — the kernel owns model choice and explains it back in the result bundle (ADR-0002). See contracts/README.md and DOMAIN-MODEL §6.",
  "type": "object",
  "required": ["contract_version", "intent", "observation_units", "observations", "variables"],
  "additionalProperties": false,
  "properties": {
    "contract_version": {
      "type": "string",
      "const": "v0",
      "description": "Pins the message to this schema version. Additive within a major version; breaking changes bump the major and live in a new vN/ directory."
    },
    "analysis_request_id": {
      "type": "string",
      "description": "Optional caller-supplied id, echoed in the result bundle for correlation. Opaque to the kernel."
    },
    "intent": {
      "type": "string",
      "enum": ["selection", "comparison", "prediction"],
      "description": "What the breeder is analyzing FOR. The kernel turns intent + data into a model choice: 'selection' -> genotype random -> BLUPs; 'comparison' -> genotype fixed -> BLUEs; 'prediction' -> uses the relationship structure (A/G/H) to predict un/under-tested material. The web tier MUST NOT send a formula or model form (ADR-0002)."
    },
    "scope": {
      "type": "object",
      "description": "Deliberate data-scoping metadata (DOMAIN-MODEL §2.2 discovery isolation). What material feeds a model is an expert choice, not an accident of the table. Informational in v0; the web tier is responsible for actually filtering observations before sending.",
      "additionalProperties": false,
      "properties": {
        "segment_id": { "type": ["string", "null"], "description": "The target Segment this analysis serves, if any. null = no commercial segment (e.g. discovery/unadapted screening), which excludes it from segment-pipeline training sets." },
        "stage": { "type": ["string", "null"], "description": "The pipeline Stage this analysis pertains to, if scoped. Program-defined ordinal label." },
        "is_discovery": { "type": "boolean", "default": false, "description": "True for discovery/unadapted-material trials; signals the result should not feed segment-pipeline predictions." }
      }
    },
    "variables": {
      "type": "array",
      "description": "The ObservationVariables (Trait × Method × Scale, BrAPI-Phenotyping) referenced by observations. Declares which are to be analyzed and how each is treated.",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["variable_id", "name", "data_type"],
        "additionalProperties": false,
        "properties": {
          "variable_id": { "type": "string", "description": "Stable id used as the key in observations." },
          "name": { "type": "string", "description": "Human-readable trait name (e.g. 'Yield', 'Brix')." },
          "data_type": { "type": "string", "enum": ["numeric", "ordinal", "nominal", "date", "boolean"], "description": "Drives how the kernel can model the variable. MVP analyzes 'numeric'." },
          "unit": { "type": ["string", "null"], "description": "Measurement unit, if any." },
          "analyze": { "type": "boolean", "default": true, "description": "Whether this variable is a response to be fit. False = present for context/gating only." }
        }
      }
    },
    "observation_units": {
      "type": "array",
      "description": "The plots/plants (BrAPI ObservationUnit). Each carries its germplasm reference and its AS-PLANTED layout position — the actual physical arrangement, gaps and all (ADR-0006). The kernel uses these coordinates for spatial correction.",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["observation_unit_id", "germplasm_id"],
        "additionalProperties": false,
        "properties": {
          "observation_unit_id": { "type": "string", "description": "Stable id; the join key for observations." },
          "germplasm_id": { "type": "string", "description": "The genotype/entry on this unit (BrAPI Germplasm). Multiple units share a germplasm_id across reps/environments." },
          "environment_id": { "type": ["string", "null"], "description": "Which environment (Location × Season) this unit belongs to. Required for MET/GxE; null/constant for single-trial." },
          "layout": {
            "type": "object",
            "description": "As-planted physical position. Provide row/col (and optionally range/pass) for spatial models. Absent or partial layout falls back to non-spatial analysis (the kernel decides and says so).",
            "additionalProperties": false,
            "properties": {
              "row": { "type": ["integer", "null"], "description": "Field row index (spatial Y)." },
              "col": { "type": ["integer", "null"], "description": "Field column index (spatial X)." },
              "range": { "type": ["integer", "null"], "description": "Breeder-casual synonym some programs use for row-blocks; carried through for as-planted fidelity." },
              "pass": { "type": ["integer", "null"], "description": "Breeder-casual synonym some programs use for the orthogonal direction." },
              "block": { "type": ["string", "null"], "description": "Design block/incomplete-block id, if any." },
              "rep": { "type": ["string", "null"], "description": "Replicate id, if any." }
            }
          }
        }
      }
    },
    "observations": {
      "type": "array",
      "description": "Long format: one entry per (observation_unit × variable) measured value. The kernel pivots as needed. Missing combinations are simply absent (unbalanced data is expected and handled).",
      "items": {
        "type": "object",
        "required": ["observation_unit_id", "variable_id", "value"],
        "additionalProperties": false,
        "properties": {
          "observation_unit_id": { "type": "string" },
          "variable_id": { "type": "string" },
          "value": { "type": ["number", "string", "null"], "description": "The measured value. Numeric for quantitative traits; string for categorical; null for explicit missing." }
        }
      }
    },
    "design": {
      "type": "object",
      "description": "Design HINTS, not a model spec. What the breeder believes the design was; the kernel validates against the actual layout and chooses the model. Optional — the kernel can infer from layout (ADR-0002/0006).",
      "additionalProperties": false,
      "properties": {
        "declared_design": { "type": ["string", "null"], "enum": ["rcbd", "alpha_lattice", "row_col", "augmented", "p_rep", "crd", "unreplicated", "unknown", null], "description": "What the breeder says the design is. The kernel treats this as a hint to confirm, not an instruction." },
        "is_multi_environment": { "type": ["boolean", "null"], "description": "Hint that this is a MET (genotypes across environments). The kernel confirms from environment_id counts." }
      }
    },
    "relationship": {
      "type": "object",
      "description": "The relationship structure among genotypes — a FIRST-CLASS engine input, not a fork (DOMAIN-MODEL §6). A/G/H BLUP are one model with different relationship matrices (Mrode; Bernardo). MVP fills 'identity'.",
      "default": { "type": "identity" },
      "additionalProperties": false,
      "required": ["type"],
      "properties": {
        "type": { "type": "string", "enum": ["identity", "A", "G", "H"], "description": "identity = no kinship (MVP); A = pedigree-based; G = marker-based; H = single-step (pedigree+markers). A/G/H are MAPPED, not built — present so the seam anticipates M5–M6 without a contract break." },
        "source_ref": { "type": ["string", "null"], "description": "For A/G/H: a reference the kernel resolves to the matrix or its inputs (pedigree set / marker set). Unused for 'identity'. Transport of large matrices is an OPEN operational detail (see README)." }
      }
    },
    "objective": {
      "type": ["object", "null"],
      "description": "The selection objective derived from the target Segment's TPP and the (Segment × Stage) SelectionCriteria (DOMAIN-MODEL §2.4). Drives gating and the index(es). Optional: absent for pure comparison/prediction runs.",
      "additionalProperties": false,
      "properties": {
        "gates": {
          "type": "array",
          "description": "Independent culling thresholds on must-have traits (DOMAIN-MODEL §2.4). A candidate failing any gate is excluded regardless of index. Gates tighten with Stage.",
          "items": {
            "type": "object",
            "required": ["variable_id", "operator", "threshold"],
            "additionalProperties": false,
            "properties": {
              "variable_id": { "type": "string" },
              "operator": { "type": "string", "enum": [">=", "<=", ">", "<", "==", "!=", "in", "not_in"] },
              "threshold": { "description": "Number for quantitative gates; string/array for categorical (e.g. accepted fruit types).", "type": ["number", "string", "array"] }
            }
          }
        },
        "index_weights": {
          "type": "array",
          "description": "Per-trait selection modes + weights for the selection index. Used by the transparent weighted index (sum-to-100% alignment tool) and, with the genetic covariance the kernel estimates, the Smith–Hazel / desired-gains index (ADR-0006).",
          "items": {
            "type": "object",
            "required": ["variable_id", "weight"],
            "additionalProperties": false,
            "properties": {
              "variable_id": { "type": "string" },
              "mode": {
                "type": "string",
                "enum": ["max", "min", "target"],
                "description": "Selection mode for this trait (ADR-0006). 'max' = higher is better (merit = +z); 'min' = lower is better (merit = −z); 'target' = an optimum where deviation from `target` is penalized quadratically (merit = −((value−target)/sd)², in genotype-sd units, so far-from-target genotypes are increasingly discounted). Authoritative when present. If absent, the kernel derives it from `direction` (+1→max, −1→min), defaulting to 'max'."
              },
              "target": { "type": ["number", "null"], "description": "The optimum value, on the trait's own scale, for mode='target'. Required when mode='target'; ignored for max/min." },
              "direction": { "type": "integer", "enum": [1, -1], "description": "Legacy shorthand for max/min: +1 = higher is better, −1 = lower is better. Retained for back-compat and as the weights_used echo; `mode` supersedes it and is preferred for new producers. Not used for mode='target'." },
              "weight": { "type": "number", "minimum": 0, "description": "Relative importance. For the transparent index the web tier may normalize weights to sum to 1 (100%) as an alignment device." },
              "desired_gain": { "type": ["number", "null"], "description": "Optional target gain for a desired-gains index (alternative elicitation to economic weights). The index-screen default — desired-gains vs economic weights — is an OPEN product detail (ADR-0006)." }
            }
          }
        }
      }
    },
    "options": {
      "type": "object",
      "description": "Non-scientific execution knobs. Nothing here may change WHICH model is chosen — that is the kernel's job (ADR-0002).",
      "additionalProperties": false,
      "properties": {
        "compute_genetic_correlations": { "type": "boolean", "default": false, "description": "Whether to estimate the genetic-correlation matrix across analyzed traits (M1+)." },
        "compute_stability": { "type": "boolean", "default": false, "description": "Whether to compute GxE/stability views for MET (M1+)." }
      }
    }
  }
}
